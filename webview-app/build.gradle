// Root build file. Plugin versions match the existing twa-build toolchain
// (AGP 8.9.1 / Gradle 8.11.1) so the same JDK 17 + Android SDK builds both.
plugins {
    id 'com.android.application' version '8.9.1' apply false
}
